<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Profile</title>
  <link rel="stylesheet" href="styles/styles.css" />
</head>
<body>

  <!-- Edit Profile Form -->
  <div id="edit-profile-form">
    <div class="container">
      <h2>Edit Profile</h2>

      <!-- Display message if needed -->
      <% if (message) { %>
        <div class="form-message"><%= message %></div>
      <% } %>

      <!-- Form to edit profile information -->
      <form action="/edit-profile" method="POST">
        <label for="firstname">First Name:</label>
        <input type="text" id="input-firstname" name="firstname" value="<%= profile.firstname %>" required /><br>

        <label for="lastname">Last Name:</label>
        <input type="text" id="input-lastname" name="lastname" value="<%= profile.lastname %>" required /><br>

        <label for="email">Email:</label>
        <input type="email" id="input-email" name="email" value="<%= profile.email %>" required /><br>

        <label for="phone">Phone Number:</label>
        <input type="tel" id="phone" name="phone" value="<%= profile.phone %>" placeholder="123-456-7890" pattern="[0-9]{3}-[0-9]{3}-[0-9]{4}" required /><br>

        <label for="birthdate">Birthdate:</label>
        <input type="date" id="birthdate" name="birthdate" value="<%= profile.birthdate %>" required /><br>

        <label for="gender">Gender:</label>
        <select id="gender" name="gender" required>
          <option value="" disabled>Select</option>
          <option value="Male" <%= profile.gender === "Male" ? "selected" : "" %>>Male</option>
          <option value="Female" <%= profile.gender === "Female" ? "selected" : "" %>>Female</option>
          <option value="Choose not to answer" <%= profile.gender === "Choose not to answer" ? "selected" : "" %>>Choose not to answer</option>
        </select><br>

        <label for="city">City:</label>
        <input type="text" id="city" name="city" value="<%= profile.city %>" /><br>

        <label for="state">State/Province:</label>
        <input type="text" id="state" name="state" value="<%= profile.state %>" /><br>

        <label for="zip">ZIP/Postal Code:</label>
        <input type="text" id="zip" name="zip" value="<%= profile.zip %>" /><br>

        <button type="submit">Save Changes</button>
      </form>
    </div>
  </div>

  <!-- <script src="scripts/scripts.js"></script> -->
</body>
</html>
